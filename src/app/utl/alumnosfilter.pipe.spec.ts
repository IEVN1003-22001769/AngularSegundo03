import { AlumnosfilterPipe } from './alumnosfilter.pipe';

describe('AlumnosfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AlumnosfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
